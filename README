Steps to make the plots:
 1. Install R and the required packages. The exact version used are documented in `renv.lock`.
 2. Add the data files from https://doi.org/10.6084/m9.figshare.19698286 to the data directory.
 3. Run with `snakemake`.
